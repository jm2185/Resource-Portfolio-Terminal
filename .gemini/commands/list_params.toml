description = "List cockpit-editable tunables (value, default, range, overridden)"
prompt = """
Call the list_params MCP tool and present a compact table: key, current value, default, range,
and whether it is overridden. Then note that /set_param <key> <value> changes one and /save_scenario
stores what-if presets. If the engine is offline, say so.
"""

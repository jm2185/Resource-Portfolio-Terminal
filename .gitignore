# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/
env/

# Phase 6 ingestion: generated cache only (data/manual_overrides.csv stays tracked)
data/ingestion_cache.json

# Dynamic config overlay (engine-owned SQLite; local runtime state, not tracked)
data/dynamic_config.sqlite*

# Phase 7.1 ingestion: generated HTTP response cache (requests-cache sqlite + WAL/SHM)
.cache/ingestion_http_cache.sqlite*

# Sensitive / Config Files
v3_config.json
v5_config.json
v4_config.json
*.env
holdings-report-*.csv
FRED_API_KEY

# Flutter / Dart
**/build/
**/ios/Pods/
**/android/.gradle/
**/flutter_*.log
.dart_tool/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.iml

# Logs
*.log
logs/
firebase-debug.log

# MCP server runtime artifacts (mcp_server/) — code is tracked, runtime state is not
.mcp_logs/
.mcp_backups/

# Antigravity/Gemini local config is ignored, EXCEPT the shared project slash commands.
.gemini/*
!.gemini/commands/
gha-creds-*.json

# Antigravity headless research outputs (local artifacts)
research/

# secrets (FMP key etc.) — never commit
.env
data/fmp_cache.json
data/market_cache.json
data/engine.log
data/engine.pid

# cockpit Agent Hub: user-saved prompt templates (local runtime state)
data/cockpit_commands.json

# cockpit scheduler: recurring-job store + agents' review drafts (local runtime state)
data/cockpit_jobs.json
data/agent_drafts/

# Runtime caches (macro / market state — local only)
.cache/*.json

# P2.1 rates dashboard: daily rate-snapshot history (runtime telemetry, accrues locally)
data/rates_history.jsonl

# Agent Hub QUEST-LOG feed: durable agent-activity stream (rolling runtime record, persists per deploy)
data/agent_activity.jsonl

# Hub chat tree: persisted conversations so chats survive a restart (rolling, per-deploy runtime state)
data/conversations.json

# SOURCED / ACCUMULATED state — TRACKED. These are NOT regenerable caches (the Tier-2 pass wrongly
# untracked them by the "cache" in a filename; pulling that deletion wiped GROY's sourced fundamentals
# and collapsed its floor). Kept in git:
#   data/research_cache.json   — hand-sourced, provenance-stamped filings fundamentals (book/NAV/oz)
#   data/living_memory.jsonl   — the append-only audit trail (council verdicts, notes, conviction)
#   data/valuation_ledger.jsonl— the graded valuation track record (stamped over time, non-regenerable)
#   data/price_history.json    — the replay ground-truth store

# Genuinely regenerable/ephemeral — never tracked:
data/matrix_bench.json
data/matrix.pid
data/engine.sha
data/catalysts.archive.csv
data/matrix_logos/

# Dated one-off run snapshots (_v0_run.py stamps terminal_state_<date>.json)
terminal_state_*.json

# Regenerable preview art (scripts/tools_preview_conviction.py)
conviction_card_preview.png

# PREDICT arb scanner runtime artifacts (the worker's universe cache; the append-only fired
# ledger and sourced fair-values are DATA the operator may choose to commit — not ignored)
data/predict_universe.json
